# 项目上下文

Atha 是本地优先的高保真个人阅读系统：阅读是入口，引用消息是长期阅读记忆的最小单位。产品目标、架构和当前实现分别由当前文档所有者维护；本文件只定义它们的职责与目录边界。

## 当前稳定目标

- 以 WebView2 浏览器呈现作为唯一阅读渲染路径，先做常规优化，再由真实功能与性能证据驱动后续工作；
- 不可信书籍默认不能执行脚本或访问网络；
- 引用必须能回到书籍版本与原文，并保留历史渲染快照；
- 用户数据本地优先、可迁移、可导出；AI 不是产品成立前提。

## 稳定语言

**Message**：
一条稳定的阅读反应；可以只是原文标注，也可以包含笔记、回复和对其他消息的引用。标注与笔记是 Message 的阅读器投影，不是另一套事实。
_Avoid_：把 annotation、note 和 message 当作三个独立领域对象。

**SourceAnchor**：
Message 回到当前书籍上下文的位置与原文身份；唯一重锚可以更新当前导航位置，但不能改写历史呈现。
_Avoid_：页码、只保存选中文字。

**SourceSnapshot**：
Message 创建或重选原文时保存的不可变历史呈现，包括 HTML、CSS、呈现参数和所需资源。
_Avoid_：运行时 DOM 副本、当前书籍样式。

**Conversation**：
由一条引用原文的根 Message 开始，并围绕它组织回复和引用关系。
_Avoid_：脱离书籍的通用聊天室。

## 文档所有权

| 事实 | 唯一当前所有者 |
| --- | --- |
| 启动顺序和硬规则 | `AGENTS.md` |
| 稳定目标、目录和所有权 | `CONTEXT.md` |
| 当前执行指针 | `docs/ACTIVE.md` |
| 当前文档注册表和最小上下文 | `docs/INDEX.md` |
| 任务分类与变更生命周期 | `docs/workflow/PROTOCOL.md` |
| 全局工作流的项目契约与检查 gate | `docs/agents/workflow.md` |
| 产品目标和体验边界 | `docs/product/OVERVIEW.md` |
| 长期架构与领域边界 | `docs/architecture/` |
| 已实现事实和机器契约 | `docs/codebase/` 与真实代码/测试 |
| 当前跨模块变更 | `docs/changes/*.md` |
| 当前未决技术研究 | `docs/research/*.md` |
| 已关闭计划、评审和研究 | Git 历史；迁移期旧目录仅作兼容档案 |

## 目录边界

| 路径 | 职责 |
| --- | --- |
| `backend/` | 平台无关后端与资源边界 |
| `reader/` | WebView2 阅读 host 与唯一阅读页实现 |
| `p0/` | 不进入正式产品的技术验证 |
| `docs/` | 当前规则、事实所有者、活动变更和未决研究 |
| `artifacts/local/` | Git 忽略的本机性能与验收证据 |

发生冲突时，先服从用户当前指令和 `AGENTS.md`；机器行为以真实代码、测试和正式命令为准；产品与流程意图以当前所有者和已接受的活动 change 为准。旧文档只解释原因，不能覆盖当前事实。
