# Message Conversation Design QA

## Source Visual Truth

- 选定概念图：本任务对话中由用户确认的概念 1；原始图片属于本机临时设计证据，不登记为项目事实。
- 后续产品约束：回复与更多位于消息下方；被回复消息位于正文上方；不显示“回复了一条笔记”等标签，只保留大引号与摘要；允许多个直接引用，但不展开被引笔记自身的回复或引用；当前默认使用 Atha 样式，微信、Telegram、QQ 与自定义主题暂缓。
- 富文本输入参考图：用户在本任务对话中提供的全屏编辑器截图。参考其顶部快捷工具栏、自增高输入与全屏输入层级，不复制尚未批准的 AI、图片、表格、公式、表情或删除功能。

## Implementation Evidence

- 浅色、深色、更多菜单和同图对照均在真实 Tauri/WebView2 中生成；图片保存在 Git 忽略的本机验收目录，不把用户目录或临时文件登记为项目事实。
- 半屏对话、两层可视工具栏和 Markdown 输入截图保存在 Git 忽略的项目本地验收目录 `.tmp/`，不作为长期项目事实。

## Viewport And Normalization

- 实现视口为 430 × 820 CSS px、devicePixelRatio 2，截图为 860 × 1640 px；这是当前 Tauri 窗口的最小移动端视口。
- 概念图为 853 × 1844 px，没有可验证的 CSS 视口或密度元数据；对照时按宽度缩放为 426 × 922 px，不裁切。
- 同图对照把概念图与实现图以近似相同显示宽度并排。两者纵横比不同，因此只判断标题、原文入口、消息层级、直接引用、操作区和编辑区，不把可视消息数量差异误判为实现问题。
- 富文本参考图为 910 × 1183 px，没有 CSS 视口或密度元数据；实现全屏输入为 860 × 1640 px，对应 430 × 820 CSS px、devicePixelRatio 2。两者纵横比不同，按完整画面共同输入检查工具栏层级、正文占位和底部发送入口，不做伪精确像素对齐。

## State And Interaction Coverage

- 同一对话包含根笔记、一级回复和第二条回复；最后一条消息同时直接关联父消息和另一条消息，因此展示两个独立引用块。
- 点击引用块可跳到当前对话中的目标消息并短暂高亮；点击更多以顶层 Popover 展示编辑、删除、修订历史和引用关系，不被输入区裁切。引用关系合并回复父项与额外直接引用，并在反向查询中统计两类直接边。
- 从全屏笔记页进入对话、返回原文、回复入口和输入框均在真实 Tauri/WebView2 中检查；输入区不再显示与回复重复的“引用其他消息”选择器。
- 浅色和深色均通过真实“主题”控件切换；设置值、根主题和阅读器语义颜色同步。深色发送图标保持白色，不再使用画布前景色。
- Agent Browser 的控制台和页面错误为空。
- 新增或编辑笔记会打开半屏对话并定位根消息；真实指针拖拽把高度从 459.20 px 调整到 552.20 px，轻点拖拽条和标题栏按钮均可进入全屏。
- 输入区随三段正文增高到紧凑上限并出现全屏按钮；全屏输入占用整个应用层，工具栏位于顶部、发送位于底部，返回紧凑输入不会关闭对话。
- 对话标题栏只保留返回与全屏；没有收起对话、三点菜单、对话导出或共享入口。
- 全屏工具栏拆成输入模式与文字格式两层，430 × 820 CSS px 下七个格式入口均完整可见；Markdown 模式可转换标题、粗体和列表，未支持的四级标题会保留输入、显示错误并禁用发送。

## Required Fidelity Surfaces

- 字体与排版：使用现有系统中文字体与 Lucide 图标；标题、正文、时间和辅助操作层级清楚，引用摘要不会与回复正文混为一体。
- 间距与布局：半屏浮层保留正文上下文；固定标题和原文入口，消息区独立滚动，编辑区固定在底部；全屏输入中两层工具栏、正文和发送入口互不遮挡，格式入口不再被视口右边缘裁切。
- 颜色与令牌：对话只消费 `--message-*` 语义令牌，并由现有 `--ui-*` 主题令牌驱动；浅色与深色均无写死前景造成的不可见控件。
- 图像与资产：界面没有产品图片；可见图标全部来自既有 Lucide 依赖，没有手绘 SVG、CSS 图标或占位资产。
- 文案：引用只有大引号和被引消息自身的正文摘要；没有“回复了一条笔记”、内部 ID、路径、数据库字段或未实现的导出/共享入口。

全图对照已经可以读清消息正文、两个直接引用、时间、操作、富文本工具栏和输入内容，无需额外局部裁切。

## Findings

没有剩余 P0、P1 或 P2 问题。

- P3：概念图更轻、更疏，当前实现因显示两个直接引用而密度更高；这是用户批准的多引用状态，不应通过缩小文字或隐藏直接引用来追求表面一致。
- P3：原生滚动条仍可见；它提供明确滚动反馈，后续只有在主题系统确定统一滚动条策略时再调整。

## Comparison History

1. P1：早期实现把回复关系写成标签并把引用放到正文下方；现已按用户约束改为“大引号 + 摘要”，所有直接引用位于当前消息正文上方。
2. P1：消息更多菜单最初作为列表内绝对定位元素，被底部编辑区裁切；改用原生 Popover 顶层后，四项操作均完整可见。
3. P2：首轮深色检查直接修改根节点，制造了“设置为浅色、外壳为深色”的非法 QA 状态；这不是产品主题路径。重新只通过真实主题控件切换后，设置值、外壳和对话颜色同步，浅色与深色截图均通过。
4. 修复后以 430 × 820、DPR 2 的相同对话内容重拍浅色实现图并重建同图对照；复查未发现新的 P0、P1 或 P2。
5. 关系回顾曾只统计额外 `MessageReference`，遗漏回复父项；真实三消息对话现依次返回 `0/2`、`1/1`、`2/0` 的正向/反向直接关系，并移除了输入区重复的“引用其他消息”选择器。
6. P1：首版全屏输入受半屏浮层的变换和底部对齐影响，只占半屏且正文贴底；移除变换包含块并在全屏状态拉伸编辑区后，430 × 820 CSS px 画面完整占屏，正文从工具栏下方开始。
7. P2：对话标题栏曾保留“导出对话”和“收起对话”菜单；按用户确认删除菜单、事件绑定和折叠样式，复拍半屏图只保留返回、标题与全屏。
8. P2：单行富文本工具栏在最小移动视口裁掉右侧入口；按用户确认改为两层，第一层放输入模式与撤销/重做，第二层完整显示七个格式入口。复拍可视与 Markdown 两种状态后没有裁切或遮挡。

## Implementation Checklist

- [x] 回复与更多位于消息下方；
- [x] 一个或多个直接引用位于正文上方；
- [x] 被引消息已有引用不递归展开或写入；
- [x] 消息菜单不被滚动区或编辑区裁切；
- [x] 浅色、深色、引用跳转、输入和菜单状态通过真实 WebView2 检查；
- [x] 同图视觉对照完成；
- [x] 半屏、连续拖拽、对话全屏、输入自增高与输入全屏通过真实 WebView2 检查；
- [x] 对话标题栏没有收起、导出或共享入口；
- [x] 富文本参考图与全屏实现已在同一视觉输入中比较。
- [x] 两层工具栏与 Markdown/可视双向切换通过真实 WebView2 检查。

final result: passed

---

# 离线书架设计 QA

## 输入与归一

- 参考：`fixtures/local/weread/bookshelf-grid.jpg`、`bookshelf-progress.jpg`、`bookshelf-selection.jpg`，均为 582 × 1280；
- 实现：`artifacts/local/screenshots/library-android-default.png`、`library-android-progress.png`、`library-android-selection.png`，来自 `Atha_API_35_16K` 的 1344 × 2992 真实应用帧；
- 对比：`library-compare-default.png`、`library-compare-progress.png`、`library-compare-selection.png` 将两侧统一为 1280 px 高并排检查；
- 系统栏复验：`reader-system-bars-themes-contact.png` 并排记录同一最终 APK 的 light、paper、dark、system-dark 与 system-light 阅读态；
- 状态对应为默认书架、进度页和已选择一本。实现只使用用户本机书籍，不把书名、作者、路径或内容写入验收记录。

## 可见差异与处理

- P1：首轮 Android 暗色页仍使用深色系统状态栏图标。已用受限的原生 bridge 按黑色书架及 system / light / paper / dark 阅读主题同步系统栏图标明暗，复验后时间、网络和电量图标均清晰可见；
- P2：冷启动后的第一张模拟器 `screencap` 偶尔早于 WebView 合成完成。正式门改为先走完实际视图交互、回到对应状态并等待两秒再取证；最终三态帧完整；
- 参考中的书城、推荐、更新、分类、私密阅读、分享、有声书和底部在线导航按产品范围删除；实现保留本地搜索、默认 / 进度 / 书名 / 作者、导入、选择和批量移出；
- 参考使用满书架，验收使用一个真实本地样本。三列尺寸、留白、文字层级、蓝色激活态、选中标记和底部危险操作的位置仍可直接比较。

## 结论

默认、进度、选择三态未发现未关闭的 P0、P1 或 P2。360 × 800、412 × 915、768 × 1024、1280 × 900 的本地渲染门和 Android 1344 × 2992 真应用门均通过；交互、触控、安全区、状态栏和空态由正式脚本覆盖。

final result: passed
